import os

##if(not os.path.exists("data")):
  # os.mkdir("python code")
# for i in range(0, 100):
  #  os.mkdir(f"data/Day{i + 1}")
    # os.rename(f"date/Day{i + 1}" f"date/tutorial{i + 1}")
folders = os.listdir("python code")
    # print(folders)
for folder in folders:
     print(folder)